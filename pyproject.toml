[project]
name = "dse-sim"
version = "0.0.1"
requires-python = ">=3.8"

dependencies = [
    "pandas",
    "termcolor"
]

[tool.setuptools]
package-dir = {"" = "src"}  # Source code is located in 'src' directory

[tool.setuptools.packages.find]
where = ["src"]  # Look for packages inside 'src'
